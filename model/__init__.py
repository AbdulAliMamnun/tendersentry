"""Learned bid-recommendation model: data foundation, features, ranking.

Trains only on local data with local embeddings. Nothing in this package calls a
hosted model API, so a training run costs nothing and is reproducible offline.
"""
